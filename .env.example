# Use "rule" for the offline Jev-contract simulator or "jev" for a real
# TypeSafe-compatible endpoint. The browser never receives this key.
DECISION_PROVIDER=rule
TYPESAFE_API_KEY=
TYPESAFE_BASE_URL=https://api.typesafe.ai
TYPESAFE_MODEL=jev-latest
PORT=8787
